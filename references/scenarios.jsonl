{"name":"mass-high-similarity-sponsor-routing","description":"Fan out over all high-similarity event sources, retain only cited sponsor activations inside the rolling past year that match Bob's approved category profile, and preserve the sponsorship property title and date.","given":"The campaign comparable-event universe, Bob's August 11 sponsor competitor profile, live public event pages, and a declared as-of date.","when":"The operator runs npm run discover -- --mass.","then":"The mass result lists every qualifying institution with cited activation evidence, routes unclear categories or dates to review, and emits accepted rows through the ordinary target gates.","validates":["high-similarity event fan-out","rolling 365-day window","category-profile mapping","evidence lineage","same target gates"]}
{"name":"title-search-to-profile-resolution","description":"Use a cited sponsor side activation owner's job title to discover and rank public LinkedIn candidates at the same company, then check up to three exact profiles.","given":"A qualifying sponsor activation with a quoted role title and general web search results that may contain exact LinkedIn profile URLs.","when":"The title to person phases run.","then":"Search hits remain candidates. The system checks up to three ranked profiles and resolves the person only after retrieval verifies the current company and title.","validates":["general title search","LinkedIn result parsing","title similarity ranking","exact profile retrieval gate","candidate versus resolved identity states"]}
{"name":"departed-person-current-employer-branch","description":"Follow a confirmed person who left the event sponsor to their current employer and add that institution instead of keeping the stale sponsor-person row.","given":"An exact retrieved profile whose name matches the ranked candidate but whose current employer no longer matches the source sponsor, plus a canonical current-employer brand domain.","when":"The one-hop destination branch runs.","then":"The source sponsor row is withheld, the current employer is added with the person's current title and exact profile, and the old event activation remains provenance rather than destination sponsorship evidence.","validates":["exact person identity check","current employer brand resolution","one-hop routing","source row replacement","provenance without false sponsorship attribution","ordinary target gates"]}
{"name":"keyless-mass-plan","description":"Keep the full mass route inspectable when Context.dev credentials are absent.","given":"A fresh clone with no CONTEXT_DEV_API_KEY.","when":"The operator runs npm run discover -- --mass --dry-run.","then":"The command exits successfully and writes a bounded event operation plan, rolling window, credit estimate, and dynamic title-search/profile-resolution stages without claiming any call executed.","validates":["keyless-safe planning","credit visibility","no false execution claims"]}
